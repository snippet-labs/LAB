'use client';

// UI components
import { BackgroundBeams } from '@/components/ui/background-beams';

const Knowus = () => {
  return (
    <div className="relative h-screen w-full bg-neutral-950 flex flex-col items-center justify-center antialiased">
      <BackgroundBeams />
    </div>
  );
};

export default Knowus;
